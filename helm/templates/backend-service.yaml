apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: backend
  ports:
- name: http
  port: 5000
  protocol: TCP
  targetPort: 5000
  type: ClusterIP